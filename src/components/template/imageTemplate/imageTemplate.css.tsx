import { style } from "@vanilla-extract/css";

export const imageTemplate = style({
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
  width: "100%",
});

export const imageConatiner = style({
  width: "100%",
});
